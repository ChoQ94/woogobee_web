# Neon Postgres 연결 문자열
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=verify-full&channel_binding=require"

# Auth.js 세션 암호화 키 — `npx auth secret` 로 생성
AUTH_SECRET=""

# Google Cloud Console > Credentials > OAuth 2.0 Client ID
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
